# Development / test dependencies for the generator_control test suite.
# NOTE: gpiozero is deliberately NOT listed -- it requires real Raspberry Pi
# hardware (lgpio) and is mocked in tests/conftest.py before import.
flask
werkzeug
pytest
pytest-cov
# Web Push: the network POST is mocked in tests, but these imports must resolve so the
# real VAPID signing + aes128gcm encryption path (py-vapid + http-ece) and the send code are
# importable/exercised. requests is mocked at module.requests.post. (We use these directly
# rather than the pywebpush wrapper, which has no Raspberry Pi OS apt package.)
py-vapid
http-ece
requests
